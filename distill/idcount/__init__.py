"""身份留存计数(id-count)——M5 P-probe 的客观计数标注工具。

与 `distill/blind_eval/` 是两回事,不要混着改:blind_eval 问的是"哪张更好"
(偏好比较,双图 A/B),这里问的是"这个参考主体的身份保住了没"(单图是/否计数)。
两者的判读心理不同,混在一份工具里会互相干扰标注者的心智模型,所以另起目录。

复用 blind_eval 的部分只有两处、且都是刻意的:`pairing.asset_tag`(图片 URL
版本戳机制,防的是同一个坑)和 `report.wilson`(区间估计,没有理由重新实现)。
"""
