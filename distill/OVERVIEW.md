# 多参考自蒸馏 · 实验主线(人类可读)

> **这份文件的定位**:按时间讲清楚每一步**为什么做、怎么做、数据从哪来、量到了什么、
> 把下一步推向了哪**。所有承重数字都在,但推导细节、预登记原文、订正注记全部留在
> `DISTILL_PLAN.md`(1771 行,归档)。**两者冲突时以 `DISTILL_PLAN.md` 为准**——
> 那里是预登记的法定文本,这里是叙事。
>
> 最后更新 **2026-08-04**(会诊二之后)。每完成一个实验就更新本文件。

---

## 0. 一句话现状

隔离注意力能带来 **1.672× 推理加速**,这是无条件的工程事实;但"加速且质量对齐 teacher"
这个原命题**已被 M4 的数据证伪**,主命题因此换成**消融**:同数据、同配方下,
**隔离相对全注意力的代价是多少**(= 臂B vs 臂A)。臂 A 已训完并判读,臂 B 待跑。

---

## 1. 我们要什么

UNO 的多参考推理里,每张参考图都要和噪声、文本做全注意力。把参考段改成**隔离注意力**
(ref 只自注意 + t=0 调制)后可以缓存 KV,实测 **1.672×** 加速。代价是官方权重不认这个结构,
必须重训。整个项目就是在回答:**这笔重训要花多少、买回多少**。

---

## 2. 模型家谱

主干 `FLUX.1-dev` 全程冻结,下面全是 LoRA rank 512。**真正训练出来的只有 4 份权重**:

| 权重 | 来路 | 步数 | 隔离 | 备注 |
|---|---|---|---|---|
| `official_full` | ByteDance 官方 UNO LoRA | — | 否 | teacher。stage-2 未开源 |
| `ckpt-20000` (`ours_kv_pre`) | 我们复刻 stage-1,**随机 LoRA 起步** | 20,000 | 是 | 8×4090。**不是**从官方权重续训 |
| `post4000` | M3 蒸馏续训 `ckpt-20000` | 4,000 | 是 | 6h14m,唯一变量是数据 |
| **臂 A** (`arm_a_full`) | **官方 LoRA 为 init** + 同一份 `train_mixed.json` | 4,000 | **否** | 5h23m。量具,不是产品(全注意力无加速) |
| 臂 B (`arm_b_iso`) | 与臂 A **只差 `--ref_isolation True`** | 4,000 | 是 | **待跑** |

不是权重、只是推理开关的:`official_iso`(官方权重直接开隔离+KV)。

**两条线在主干处就分了岔**:`ckpt-20000` 是从随机 LoRA 自己复刻 stage-1 的,
所以底座差距是**继承来的**,不是 M3 造的。

**底座差距的来源(已核实)**:官方 stage-1 用 UNO-1M 全库 ∩ `score_final ≥ 4.0`
= **404,259** 对;我们磁盘只解压 split1-5、约 **5 万**对且 `convert_uno_labels.py`
**没有任何 score 过滤**,其中够官方标准的只有 **16,966** 条 ⇒ 有效满分样本 **≈24×**。
⚠️ 这是**候选解释,不是实验结论**——没有任何一个实验把质量损失归因到 score 过滤上。

---

## 3. 判读这把尺子(所有结果都建立在它上面)

- **谁判**:单标注者(你),人工盲评。**这是全流程最贵、最不可再生的资源。**
- **主指标**:非平局胜率 `S/(S+T)` + Wilson 95% CI。平局率**单列**,不并进主指标。
- **达标判据**(`M4_EVAL_SPEC.md` §8.2,commit `52b9b42` 冻结):
  `CI 下界 ≥ 0.40` **且** `n_nontie ≥ 94`。样本不足时结论是
  **「判据不适用」而非「不达标」**。
- **两条硬纪律**:① 不同批次的非平局胜率**不得并排引用**(跨批漂移 κ=0.274);
  ② 判据/预测一旦写下不许改,只能加带日期的订正注记。

**尺子的刻度(已标定,单调)** —— 这是 08-04 才补上的地基:

| 比较结构 | 平局率 | Wilson 95% CI |
|---|---|---|
| 同 seed **同权重**、异 run(几乎同图) | **98.3%** (59/60) | [0.911, 0.997] |
| 臂 A vs teacher(同 seed 异权重) | **59.4%** (114/192) | [0.523, 0.661] |
| `post4000` vs teacher(R2 重放,n=20) | 30.0% | [0.145, 0.519] |
| `post4000` vs `pre` | 28.4% (42/148) | [0.217, 0.361] |
| 零假设对(**异 seed** 同权重) | 33.3% (10/30) | [0.192, 0.512] |

平局率随真实差距**单调下降** ⇒ 标注者不是"平局倾向"型,**平局率可以当读数用**。
位置偏差:M4 50.0%、R2 50.0%、臂 A 39:39 = 50.0% —— **三批都没有左右偏好**。
自洽率:跨 4 天 55.0%(κ=0.274),场内 40 分钟 70.0%(κ=0.528)。

---

## 4. 已做的实验

### 4.1 M1–M3:造数据 + 蒸馏续训

**为什么**:官方 stage-2 的多主体合成数据与管线未开源,只能自己造。
**怎么做**:用官方 teacher 生成多主体目标图 → 人工全量标注过滤 → 混成
`train_mixed.json`(29,777 条:60% 单 ref 真实数据 + 36% 蒸馏 2-ref + 4% 蒸馏 3-ref)
→ 在 `ckpt-20000` 上续训 4000 步得到 `post4000`。
**结果**:训练零异常。**推动**:进入 M4 验收。

### 4.2 M4 全量评测(07-30 生成,判读 227 条)——原命题在这里就死了

**怎么做**:232 条五分层评测集,`post4000` vs `official_full` 成对盲评。
**结果**:

| 层 | teacher 胜 | 学生胜 | 平局 | n | 非平局胜率 | Wilson 95% CI |
|---|---|---|---|---|---|---|
| **总体** | 82 | 50 | 95 | 227 | **37.9%** | **[0.301, 0.464]** |
| S1(2 ref) | 55 | 32 | 45 | 132 | 36.8% | [0.274, 0.473] |
| S3(1 ref) | 20 | 11 | 29 | 60 | 35.5% | [0.211, 0.531] |

**CI 上界 0.464 < 0.5,符号检验 p = 0.0067**(独立复算确认)。
⇒ **「学生打平 teacher」不是"没证出来",是已被证伪。** `n_nontie = 132 ≥ 94`,判据适用,不达标。
**推动**:回退的原因必须拆开——是底座?是隔离?还是配方?于是有了后面全部工作。

### 4.3 R2 批 198 条(08-03)——一次判读同时买三个数

**为什么**:M4 只给了一个合数,而且**没人验过这把尺子有没有刻度**。
**怎么做**:三类配对**打散混成一批**,标注者看不出哪条是哪类(单独成批会自毁——
标到第五条就认出来了,判读行为随之改变):

| kind | 比什么 | 条数 | 结果 |
|---|---|---|---|
| `post_vs_pre` | post4000 vs ckpt-20000 | 148 | **95.3%** [0.894, 0.980];S1 **62:0**;S3 88.6% |
| `null_floor` | teacher vs teacher(仅噪声不同) | 30 | 平局 33.3%,胜率 45.0%,**位置偏差 50.0%** |
| `replay` | M4 已标过的原对重放 | 20 | 自洽 55.0%,**κ = 0.274** |

**三条结论**:
1. **「M3 配方有毒」被证伪** —— post4000 在每一层上都远胜自己的起点。配方不是有毒,是**不够**。
   P2(动混比 / 减半步数)这条修复路线**依据没了,作废**。
2. **尺子无位置偏好**,但很粗(n=30 时 CI 半宽 ±20pp)。
3. **尺子会跨批次漂**,且漂移全部发生在平局边界上 ⇒ 写进 SPEC §8.5-3:跨批禁止并排引用。

**推动**:回退不能记在 M3 头上 ⇒ 只剩底座与隔离 ⇒ 上 P-probe 与臂 A/B。

### 4.4 P-probe(08-03)——隔离在未重训权重上不是"变差",是"归零"

**为什么**:最便宜(纯推理 25.8 min)、而且这个组合**从没跑过**;若隔离本身代价极大,臂 B 的设计要跟着变。
**怎么做**:官方 LoRA **不重训**、直接开 `ref_isolation + kv_cache` 跑 192 条评测集,
按 SPEC §9 做**客观身份留存计数**(不是偏好盲评)。

| 变体 | per-subject 留存 | Wilson 95% CI | 速度 |
|---|---|---|---|
| `official_full` | 45/51 = **88.2%** | [0.766, 0.945] | 1.00× |
| `official_iso` | **0/51 = 0.0%** | [0.000, 0.070] | **1.672×** |

配对结构下 45 个不一致**全部同向**,反向 0 个,McNemar 精确 p ≈ 2.8×10⁻¹⁴。
先排除了"管道坏了":iso 的峰值显存**更高**(37.0 > 36.2 GB,ref token 确实在序列里)、
加速比 1.672× 与 KV 基准 1.686× 吻合、0 失败无崩坏。

**这一条推翻了可加归因模型**:同样开隔离,官方权重身份归零、`post4000` 大体保住
⇒ **隔离代价不是与权重无关的常数**,它完全由"有没有为隔离重训过"中介。
反过来这是给 M3 的正面证据:那 4000 步把隔离架构从**不可用**抬到**接近可用**。

### 4.5 归因模型改写:三边链条(08-04)

可加式作废,换成**每条边只差一个变量**的链:

```
official_full ──①配方──> 臂A ──②′隔离──> 臂B ──③底座──> post4000
  官方权重         官方init      官方init       ckpt-20000 init
  0 步             4000 步       4000 步        4000 步
  全注意力         全注意力      隔离           隔离
  ✅已测           ✅已测        ⬜待跑         ✅已测
```

由此定死三条纪律:**臂 A 测不了底座 gap**;**`臂A vs post4000` 不是链上的边**(同时差两个变量),
不为它花判读预算;**臂 B 是必经节点**,没有它 ②′ 和 ③ 一个都定不下来。

### 4.6 臂 A(08-04)——训练 + 出图 + 判读

**为什么**:导师明确要的那个消融,且它是链条第 ① 边:
**把我们的配方放在一个已对齐的底座上,会不会掉质量。**
**怎么做**:官方 LoRA 为 init(门禁① 键集合严格相等断言已过)、`--ref_isolation False`、
同一份 `train_mixed.json` 跑 4000 步 → 与 `official_full` 在**同一会话、同 seed**
出 192 条 × 2 变体 = 384 张 → 盲评 192 对。

**两个必须先查的东西**(都查了):
- 两变体 `peak_mem` **逐位相同**、median 耗时差 3.5e-5 s ⇒ 两边确实都是全注意力、都没开 KV。
  **臂 A 的读数是质量,不是速度**,`speedup 0.9992` 在本批无信息量。
- `swap_lora` **确实换掉了权重**:同 seed 同 prompt 的两列出现**构图级**差异,
  远超 run 间抖动(像素级 mean|Δ| 1.6–3.7)。必须查,因为"权重没换成功"会**伪装成**"配方无代价"。

**结果(冻结,不再改动)**:192/192,16.1 min,中位 4.0 s/对。

| | n | 臂A 胜 | teacher 胜 | 平局 | 平局率 | 非平局胜率 | Wilson 95% CI |
|---|---|---|---|---|---|---|---|
| **总体** | 192 | 31 | 47 | 114 | **59.4%** | **39.7%** | **[0.296, 0.508]** |
| S1 | 132 | 26 | 37 | 69 | 52.3% | 41.3% | [0.300, 0.536] |
| S3 | 60 | 5 | 10 | 45 | 75.0% | 33.3% | [0.152, 0.583] |

- **`n_nontie = 78 < 94` ⇒ 判据「不适用」,不是「不达标」。不许事后追加样本。**
- 符号检验 p = 0.089(独立复算确认)—— 方向偏 teacher 但**不显著**。
- S1 组合级 **ICC = +0.169**、deff 1.12(M4 那次是 −0.041)⇒ **组合内相关不恒为 0**,
  SPEC §8.3「每批必须测」这条要求兑现了价值。本批 deff 1.12 不改变任何结论。

**这里发现了一个会推向我们自己想要的结论的混杂**:预登记时写"平局率显著高于零假设对的
33.3% ⇒ 配方无代价"。但零假设对 **30/30 全部换了 seed**,而臂 A 是**共享噪声**的——
共享噪声让两张图先天更像、平局率更高,**与模型好坏无关**。
⇒ 拒绝下"配方无代价",先去补那个从没测过的天花板。

### 4.7 尺子标定批(08-04)——天花板 98.3%,边 ① ≠ 0

**为什么**:59.4% 到底贴着"完全无法分辨"的上限,还是只走了一半?
缺 **同 seed + 同权重 + 异 run** 这个条件就读不出方向。
**数据从哪来**:**免费**——M4 的 192 张 `official_full` 与臂 A 批的 192 张 `official_full`
同任务同 seed(逐条核过 192/192),只差生成会话。**GPU 成本 0。**
**怎么做**:60 条 `run_floor` + 20 条臂 A 原对重放,**混在同一场**判(天花板若在另一场测,
拿它跟 59.4% 比又成了 §8.5-3 禁止的跨批比较)。读法在判读前写死成三行。

| kind | n | 平局 | 平局率 | Wilson 95% CI |
|---|---|---|---|---|
| `run_floor` 同 seed 同权重异 run | 60 | 59 | **98.3%** | **[0.911, 0.997]** |
| `replay` 臂 A 原对重判 | 20 | 8 | 40.0% | — |

**天花板 CI [0.911, 0.997] 与臂 A 的 [0.523, 0.661] 完全不相交,Fisher p = 1.9e-10**
⇒ **预登记读法第一行触发:「配方无代价」不成立,边 ① ≠ 0。**

> 事前预期写的是"天花板落在 90–100%",**兑现**。它推翻的正是对本项目有利的那条读法。

**三个附带产出**:① 尺子的单调性第一次被验证(见 §3);
② 本栈的不可逐位复现在**人类感知层面可忽略**(59/60 判平局)——这对整个 M5 方法是正面证据。
**[2026-08-04 已上机验证]** 60 对**逐位相同 0 对**,所以 98.3% 是真实读数不是平凡数;
但分布**右偏重尾**:p50 1.561 / p90 4.718 / p95 6.546 / max 15.696,**9/60 超出
既往测的 1.6–3.7**(那是 15 张 S0 锚点的小样本低估),最大那条落进"换了 seed"的
量级带(10.4–42.9)。唯一那条非平局 `S1_039_s2` = 6.319,**排第 5/60**,而比它更散的
4 条全被判平局 ⇒ **n=1 判不了它是误判还是图真的变了**(纯噪声下落进前 5 的 p ≈ 0.083)。
**改用上界表述:标注者误判率 ≤ 1/60(Wilson 上界 8.9%),不是点估计。**
副产品:**像素差不是感知差的代理**(15.70 判平局、6.32 判非平局)——
这证明天花板必须用人量,不能算像素阈值代替;
③ 场内自洽 14/20 = 70.0%、κ = 0.528,平局 12→8 但 McNemar p = 0.219 **不显著**,
不能说尺子在这一场里漂了。

---

## 5. 现在能说什么、不能说什么

**边 ① 的正确表述(三句缺一不可)**:

> 配方把一个"98% 判不出差别"的东西变成了"59% 判不出差别",约 **40% 的条目上臂 A 与
> teacher 可分辨**。**方向未定**(47:31 偏 teacher,p=0.089 不显著)。**量级未测**。

⚠️ **两处不许说过头**(会诊二订正):
1. **不能说"用这份数据训不出质量不掉的学生"** —— 数据只证明了"训不出一个**与 teacher
   不可分辨**的学生"。**可分辨 ≠ 有代价**:一个风格漂移但同等好的模型也会被判可分辨。
2. **不能把边 ① 记在 score 过滤头上** —— ① 段是"配方"=**数据 + 4000 步**的混合体,
   没有任何实验把它归到过滤上。24× 只能以**未检验的候选解释**身份进报告。

**定性排序(不可相减)**:

```
天花板 98.3%  ──①配方──> 臂A 59.4%  ──②′隔离 + ③底座──> post4000 30.0%
```

两段量级**相当** ⇒ `post4000` 与 teacher 的差距**不能全记在隔离和底座头上**。
排序本身干净(CI 两两不相交),但 §8.5-3 禁止跨批相减,且 `post4000` 那一格 n=20。

---

## 6. 基线换了:teacher 出局,臂 A 上位

**为什么必须换**:teacher 与我们的学生之间隔着一条**与隔离无关**的鸿沟——
臂 A **不开隔离**都已经掉到 59.4% 可分辨。拿 teacher 当基线,无论臂 B 出什么,
都立不起"隔离可行"这个正面结论。而 M4 的 50/132 说明旧命题**已被证伪**,
换基线不是止损,是**本来就该做的消融**。

**新主命题 = 边 ②′**:

> 同一份数据、同一个配方、同一个 init 下,**隔离相对全注意力的代价是多少**,
> 换来 1.672× 加速。

两边同数据同配方,数据鸿沟被**完全消掉**,剩下的恰是原命题里真正关于隔离的部分。
代价是结论从"学生打平 teacher"降级为"隔离的代价是 X"。

**这属于贡献口径变更,按 §11.5 需要与导师沟通,而边 ① ≠ 0 就是当初定的那个数据触发。
沟通排在臂 B 判读之前**——若导师对换基线有异议,最后一笔判读预算不该在异议前花掉。

---

## 7. 后续计划(会诊二拍板 + 一处加固)

判读总预算从此刻起:**5 分钟(门失败)或 ~21 分钟(门通过)。没有第四个批次。**

| 步 | 做什么 | 成本 | 状态 |
|---|---|---|---|
| ~~**1a**~~ | ~~给导师发口径变更~~ | — | **✅ [2026-08-04] 导师确认:这条消融就是他原本要的做法。§11.5 的沟通条款不再阻塞** |
| **1b** | **臂 B 训练**:官方 init + 隔离 + `train_mixed.json` 4000 步 | ~6.2 h GPU | **✅ [2026-08-05] 4000 步训完**(经两次中断续训,见下"训练的两次中断") |
| **2** | 顺手两件:① `run_floor` 像素差确认(30 s);② `ls` 查 97 个未解压 split(1 min) | ~2 min | ✅ 0a 已上机(逐位相同 0 对);0b 搁置 |
| **3** | **只生成 `arm_b` 一个变体的 192 张** + 同会话补 30 张 `official_full`;对侧复用臂 A 批现图 | ~19 min GPU | **✅ [2026-08-05] 工具就绪**:`build_arm_b_tasks.py` + `arm_b_tasks.json`(192 任务/222 张),执行单 `M5_ARM_B_EVAL_RUN.md` |
| **4** | **§9 身份留存门**(判读 5 min):数 51 问,**预登记阈值 60%** | 5 min 判读 | **✅ [2026-08-05] 清单就绪**:`build_arm_b_gate.py` + `gate_items.json`(30 item / **51 问**,两路抽样交叉验过) |
| **5** | 门通过才做:最后一个 §8 批次 **222 对** | 16 min 判读 | **✅ 工具就绪**:`build_pairs.py arm-b`(条件执行) |
| **6** | 写报告 | — | — |

### 训练的两次中断(2026-08-05,只影响"怎么复现",不影响读数)

两次都死于 **SIGHUP**:`torch.distributed.elastic` 自己注册了 SIGHUP 处理器
(`multiprocessing/api.py:79 _terminate_process_handler`),**覆盖掉 nohup 设的 `SIG_IGN`**
⇒ **`nohup` 保护不了 accelerate/torchrun**,终端一断整批 worker 被杀。
第一次死在 2593 步(无 traceback),第二次死在 2585 步(有 traceback,`got signal: 1`)。
处置:`scripts/train_distill.sh` 放行 `RESUME_FROM_CHECKPOINT=latest`(commit `4247a44`,
显式文件路径会让 `global_step` 归零、4000 步变成 6000 步),启动改用 `setsid` / `tmux`。

**两处已知的续训代价**(写进 §8.5 局限):
① `train.py` 只 `load_state_dict` LoRA 权重、没有 `accelerator.load_state`
⇒ **optimizer 动量未恢复**(lr 恒定 8e-5,scheduler 不受影响);
② dataloader 从新 epoch 重采样 ⇒ **数据顺序与不中断的一次不同**。
中断前后 loss(0.3822 / 0.3822)与 s/it(5.33 / 5.33)逐项吻合,**未见损伤**。

### 步 3 的两处细节

- **不再要求"两个变体同会话重生成"**。原来那条理由已被 08-04 的标定批自己拆掉:
  跨会话差异的**感知读数就是 98.3% 平局**。省一半出图时间,配对仍是单变量
  (隔离训练 + KV 推理作为**一个部署包**,也正是最终要主张的那个配置)。
- **⚠️ 我在会诊意见上加的一条**:生成 192 张 `arm_b` 时,**顺手在同一会话里补 30 张
  `official_full`**(+2.6 min GPU)。理由:`run_floor` 要当对照,就该跟被检验的那一对
  **跨同一个会话间隔**。§11.3 步骤 1 已经量到会话漂移**不是常数**
  (smoke→M4 那段远大于 M4→08-04 那段),用 M4↔臂A 的旧对去校准
  臂B↔臂A 的新间隔是不匹配的。2.6 分钟买掉这个不匹配,值。
- **工程前置**:`eval_multiref.py` 的 `VARIANTS` 是模块级硬编码表,加 `arm_b_iso`
  **必须本地改这个文件**(手册 R0:远端不改既有 `.py`),改完 push、H800 `git pull`。
  **✅ 已完成**(08-04 加变体;08-05 加 `build_pairs.py` 的 `arm-b` 子命令,其余全是新文件)。
- **[2026-08-05 新增,预登记之外的实现细节]** 那 30 条 `run_floor` 锚点的 prompt/refs
  会在终批里**出现两次**(一次 `arm_b` 对、一次 `run_floor` 对)——躲不掉,因为天花板
  必须跨"与被检验那一对相同的会话间隔",而臂 A 批只覆盖 S1+S3 这 192 条。
  纯 md5 打散下实测**最小间距 3、5 条 <10**,标注者必然察觉。
  处置:`build_pairs.py:spread_runfloor` 只在 30 个 `run_floor` **槽位之间**做确定性重排
  (瓶颈二分图匹配,最大化最小间距),把最小间距拉到 **89**(批长 222 的 40%,
  超过既有纪律 `len//3` = 74),**槽位本身不动** ⇒ 锚点三分位分布仍是 10/13/7,尾段不聚堆。
  **WHY 不用"把锚点挤到批尾"那种分段构造**:那会让尾段锚点密度升到 40%+,
  标注者一进尾段就发现"这些几乎一模一样",**判读策略当场改变**——那是一阶风险,
  比"见过这个 prompt"的二阶风险更糟。即便如此,"同一 prompt 出现两次"**仍要写进 §8.5 局限**。

### 步 4 的门(**上机前预登记,现在就写死**)

逐字复用 P-probe 的抽样(同 30 条任务、同 `random.Random(20260803)`),
只数 `arm_b` 一个变体的 51 问。锚点 **0/51**(`official_iso`)与 **45/51 = 88.2%**
(`official_full`)都是现成的,直接可比。

| per-subject 留存(点估计) | 判定 |
|---|---|
| **< 60%** | **偏好批取消**。此时臂 B 与臂 A "一眼可辨",§8 偏好盲评名不副实(§8.5-2 在 P-probe 上已判过一次先例)。结论直接写:**4000 步补不完隔离适配,边 ②′ 承重、边 ③ 小,留存率就是读数。** 项目在此收口 |
| **≥ 60%** | 进步 5 |

判定用**点估计**,不用 CI(51 问在 60% 处 CI 约 ±13pp,用 CI 判会两边都不确定)。
⚠️ 诚实声明:这把 §9 尺子只在**极端对比**(0% vs 88%)上验证过(§11.6 明写"不许外推"),
落在 50–70% 这个带里时它最不可靠。**我们接受它作为决策规则,不把它当测量值。**

### 步 5 的批次(门通过才建)

| kind | 内容 | 条数 |
|---|---|---|
| 主 | `arm_b_iso`(key_0,被检验方)vs `arm_a_full`(key_1,基线) | 192 |
| `run_floor` | 同会话补的 30 张 `official_full` ↔ 臂 A 批的对应 30 张 | 30 |
| ~~`replay`~~ | **砍掉**:自洽率已测两次(κ=0.274 / 0.528),第三次不承重 | 0 |

新盲种、问题逐字沿用、打散排序。**这是新主命题本身的直接单变量读数。**

---

## 8. 明确砍掉的(不做清单)

| 砍掉的 | 为什么 |
|---|---|
| **`ref_size=320` 3-ref 探针** | 只影响报告里"teacher 在 3-ref 上系统性不行"一句的措辞,而 3-ref / S4 不在任何剩余承重结论里(臂 A/B 批全是 S1+S3)。处置:那句降格为"在 512 下测得,官方推荐 320,未在 320 复测" |
| **重跑 stage-1 修复底座** | 30 h GPU,且加上 `score≥4.0` 过滤后盘子从 5 万掉到 1.7 万——**样本量反而少 3 倍**,结果可能更差。本次项目**关死**这条路线 |
| **臂 A 方向问题加样本** | 31/47/114 已冻结,报告里方向就写"未定" |
| **`arm_b vs teacher` 偏好对** | 链条给定性排序就够,①+②′ 合并量不承重,省 192 对判读 |
| **臂 B 的 `replay`** | 见步 5 |
| P2(动混比/减半步数) | 两条依据都被 R2 表 ③ 推翻,不是暂缓,是依据没了 |
| P0 ckpt 扫描 / 重评 post4000 / S2、S4 / 再做 null_floor / 第三臂 | 均不承重 |
| 注意力可视化 D01–D04 | **在另一个分支上,不在本主线范围内** |

---

## 8.5 相关工作:OminiControl2(2026-08-04 读,arXiv 2503.08280)

**先记着,本次项目不动;影响的是"这活能不能写成论文"的定位。**

他们的 §3.3 就是我们的隔离注意力:*"prevent condition tokens **C** from attending to
noisy image tokens **X** ... incorporated during model training"*。**训练时就开掩码**这一点我们
和他们一致,这是他们成功的关键条件。差异有三处:

| | OminiControl2 | 我们(`ref_attention.py:82-90`) |
|---|---|---|
| ref → txt | 论文未说明 | 阻断 |
| ref_i → ref_j | 论文未说明(他们的"多条件"是同一张图的 canny+depth+inpaint+deblur,天然对齐) | 阻断 |
| **compact token representation**(条件图 2×2 下采样 `r=0.25` + 位置索引校正) | 有 | **没有**(`ref_size=512`,`r=1.0`) |
| t=0 调制 | **全文只字未提** | 有(`model.py:206-211`),归属待查 v1 |

**三个必须记住的数**(他们 Table 1,512×512 / FLUX-dev / 28 步,与我们配置几乎逐项相同):

| 配置 | 延迟 | 加速 |
|---|---|---|
| OminiControl 原版 | 13.30 s | 1.000× |
| Ours / **F.R. only**(只缓存,不压缩)| 8.18 s | **1.626×** |
| Ours(缓存 + 压缩) | 7.85 s | **1.694×** |

> **我们的 1.672× 正好夹在这两个数之间。** 加速比不是新数,已被占据(2025-03)。
> 他们宣传的 3.8–5.9× 全部来自**压缩 + 多条件 + 高分辨率**,不是缓存。

**P-probe 的定位也要降级**:他们的 "Naïve cache" 基线(不加掩码直接缓存)就是我们的
`official_iso`,实测 Deblur FID 16.381 → **45.039**、MUSIQ 71.8 → 55.7,诊断也给了
(*training-inference discrepancy*)。我们的 0/51 更狠、统计更硬,但**现象和机制他们先报了**。

### 真正的空白(这才是记这一节的原因)

他们评的四个任务 —— canny / depth / inpainting / deblur —— **全部空间对齐**。
subject-driven 全文只出现一次,在 §3.1 讲位置索引时顺带提"non-aligned tasks
(e.g., subject-driven generation) 位置索引偏移 (Δi,Δj)",**零实验、零指标**(指标里没有
DINO,没有任何身份保持项)。

这个空白有道理:对齐任务里条件是**结构约束**,ref 看不看得见主图无所谓,对应关系由位置索引
直接给定;主体驱动里条件是**要被搬运的外观**,ref 一旦看不见主图,就不知道自己会被贴到哪、
什么姿态、什么光照。

> **「隔离在空间非对齐 / 多主体任务上的代价是多少」——字面上没人量过。**
> 而我们手上有他们没有的东西:单变量对照链(臂A/臂B 只差一个 flag)+ 校准过的尺子
> (天花板 98.3% / run_floor 59.4%)。

### 以后想接着做的话,三件便宜的事

1. **降 `ref_size`**(512 → 320 / 256,≈ `r=0.39/0.25`),把压缩这一维补上。多 ref 场景条件
   token 数随主体数线性增长,**压缩收益最大的恰好是我们的场景**;而且 320 本来就是 UNO 官方推荐值。
2. **放开 `ref_i ↔ ref_j`**,做成掩码第三档。多主体绑定是否因此恢复——这是我们场景独有的自由度,
   他们论文里不存在这个问题。
3. **查 OminiControl v1** 的论文/代码,确认 `t=0` 调制是谁的设计。v2 论文完全没提这一块。

---

## 9. 报告口径

**必须放弃**:「1.672× 加速 + 质量不掉(vs teacher)」——**任何强度都不能主张**,
已被 M4 证伪(50/132,CI 上界 0.464 < 0.5,p = 0.0067)。

**可以主张**:

1. **1.672× 加速** —— 无条件的工程事实,单列;
2. **P-probe 0/51 vs 45/51** —— 隔离**必须**适配,官方权重能当 init 但不是可用模型;
3. **边 ① 三句话版** —— 可分辨 / 方向未定 / 量级未测,严格按 §5;
4. **平局率尺子的单调性验证** —— 方法学贡献,附带"跨会话重生成在感知上可忽略";
5. **24× 数据 gap** —— **仅作候选解释**;
6. 臂 B 出来后按分支补:要么「4000 步隔离适配不完成,留存率 X%」,
   要么「同数据同配方下隔离代价 = X,换 1.672×」——**后者就是新主命题的最终句**。

**旧标注全部保留有效,只改它们在论证里的角色**:M4 的 227 条从"验收批"降格为
"链条终点 `post4000` vs teacher 的历史读数"(引用必带 §8.5-3 跨批注记);
R2 的 198 条里 post-vs-pre 仍是"M3 配方确实改变了模型"的证据。**不作废任何标注。**

---

## 10. 纪律(违反就得重做)

- **预登记**:判据/预测写下后不许改,只能加带日期的订正注记(`~~删除线~~` + `**[日期 修正]**`)。
- **冻结**:声明冻结的文件只出新版本,不就地改。
- **R0(远端)**:H800 上**永不修改**既有 `.py`/`.sh`;改动一律本地做、push、远端 `git pull`。
  H800 推不上去(代理吃 POST)。
- **盲法**:走 §8 偏好盲评的批次,交付物里**不放带变体名的拼图**;开评前看过多少要写进局限。
- **单标注者**:无标注者间一致性,这是无法修复、只能声明的局限(SPEC §8.5-1)。

---

## 11. 速查

| 要什么 | 在哪 |
|---|---|
| 预登记原文、全部推导细节 | `distill/DISTILL_PLAN.md` §11(§11.11 = 会诊二决定) |
| 判据、局限声明、身份计数口径 | `distill/M4_EVAL_SPEC.md` §8 / §9 |
| 统计口径的唯一实现 | `distill/blind_eval/report.py` |
| 判读批次怎么构造的 | `distill/build_pairs.py`(`arm-a` / `arm-a-ctl` 子命令) |
| 臂 A 执行单(已完成) | `distill/M5_ARM_A_RUN.md` |
| **臂 B 执行单(当前)** | `distill/M5_ARM_B_RUN.md` |
| 原始标注 | `output/eval_arm_a/blind_annotations_m5aa*.json`、`output/eval_multiref/blind_annotations_m5r1.json`、`output/probe_iso/idcount_marks.json` |

**离线复算(任何时候可重跑)**:

```bash
python -m distill.blind_eval.report \
    output/eval_arm_a/pairs_m5aa.json \
    output/eval_arm_a/blind_annotations_m5aa.json
```
